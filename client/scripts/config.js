//This file is ignroed in gitignore, to avoid checking in apiKey etc.
const config = {
  // baseUrl: "https://flappy-nerd.azurewebsites.net/api/",
  baseUrl: "http://localhost:7071/api/",
  apiKey: "<replace with function key>"
};
